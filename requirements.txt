# Production dependencies — used in Docker image
# For local dev extras (notebooks, evaluation), see requirements-dev.txt

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# AWS Bedrock
boto3>=1.35.0
botocore>=1.35.0
tenacity>=9.0.0

# Agent orchestration
langgraph>=0.2.0
langchain-core>=0.3.0

# Vector store (local, no OpenSearch for dev)
chromadb>=0.5.5

# Data
pandas>=2.2.0

# Utilities
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0
structlog>=24.4.0

# Testing (needed by CI)
pytest>=8.3.0
pytest-asyncio>=0.23.0
